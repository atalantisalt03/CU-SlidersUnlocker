# Unlocked Sliders

Builds against your local Casualties Unknown Demo install and BepInEx assemblies.

## Requirements

- .NET SDK
- Casualties Unknown Demo installed
- BepInEx installed in the game folder

## Build and Install

Run:

```bat
build-and-install.bat
```

The script tries to find the game from Steam's install and library folders. If it cannot find it, pass the game folder:

```bat
build-and-install.bat "D:\SteamLibrary\steamapps\common\Casualties Unknown Demo"
```

You can also set `CASUALTIES_UNKNOWN_DIR` to the game folder.

## Manual Build

```bat
dotnet build UnlockedSliders.csproj -c Release -p:GameDir="D:\SteamLibrary\steamapps\common\Casualties Unknown Demo"
```

Then copy `bin\Release\netstandard2.1\UnlockedSliders.dll` into the game's `BepInEx\plugins` folder.

## Config

The config file is generated by BepInEx at runtime. It is not copied from this repo.

After the custom settings menu is opened with the mod installed, missing slider limit entries are saved to:

```text
BepInEx\config\kef.casualtiesunknown.unlockedsliders.cfg
```
